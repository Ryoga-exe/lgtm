import CopyButton from "./components/_copy-button.marko";

<section>
  <div>
    <p>LGTM 画像共有サイトです。</p>
    <p>ボタンをクリックするとランダムな画像がコピーされます。</p>
  </div>
  <div.actions>
    <CopyButton
      description="GitHub Markdown 形式でコピー"
      defaultLabel="Markdown Copy!"
      valueKey="markdown"
    />
    <CopyButton
      description="画像 URL をコピー"
      defaultLabel="Copy Image URL"
      valueKey="url"
    />
  </div>
</section>

<style>
  section {
    display: flex;
    flex-direction: column;
    gap: 2rem;
  }

  .actions {
    display: flex;
    flex-direction: column;
    gap: 1rem;
    align-items: center;
  }
</style>
